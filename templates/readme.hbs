<!-- {{SIGNATURE}}, version {{VERSION}} -->

# Welcome to **`{{PACKAGE}}`**

> [!CAUTION]
> `{{PACKAGE}}` is a Work In Progress (WIP)

## Install {{PACKAGE}}
To install `{{PACKAGE}}`, follow this:

```console
pip install .
```

## Use {{PACKAGE}}
To use `{{PACKAGE}}`, follow this:

```python
import {{PACKAGE}}

...
```
## Use {{PACKAGE}} with container (Docker/Podman)
To containerize `{{PACKAGE}}`, follow this:

```console
docker build . -t {{PACKAGE}}:{{PRJVERSION}}
docker tag {{PACKAGE}}:{{PRJVERSION}} {{PACKAGE}}:latest
docker run {{PACKAGE}}
```

## Acknowledgments
Thanks Python Community!
